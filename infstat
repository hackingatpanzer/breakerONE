local Stat = getgenv().tack -- Any statpoint u want to go into negative (I'll try to find a better vers of this)

game:GetService("ReplicatedStorage").RS.Remotes.UI.SpendSP:InvokeServer(Stat, getgenv().back)
